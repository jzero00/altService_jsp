<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="altService.sys.member.service.impl.MemberMapper">

	<select id="login" parameterType="member" resultType="member">
		SELECT * FROM altservice.member
		WHERE altNo = #{altNo}
		AND pwd = #{pwd}
	</select>
	
	<insert id="insertMemberManage" parameterType="memberManage">
		INSERT INTO altservice.comtnemplyrinfo
		(emplyr_id,	user_nm, password, password_hint, password_cnsr, area_no,
		 house_middle_telno, house_end_telno, house_adres, zip, emplyr_sttus_code)
		VALUES
		(#{emplyr_id}, #{user_nm}, #{password}, #{password_hint}, #{password_cnsr}, #{area_no},
		 #{house_middle_telno}, #{house_end_telno}, #{house_adres}, #{zip}, #{emplyr_sttus_code})
	</insert>
	
	<select id="selectMemberManageList" resultType="memberManage">
		SELECT *
		FROM altservice.comtnemplyrinfo
	</select>
	
	<select id="selectMemberManageListCnt" resultType="int">
		SELECT COUNT(*)
		FROM altservice.comtnemplyrinfo
	</select>
</mapper>